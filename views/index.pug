extends layout

block head
  div(class="banner")
    h1(class="banner-header") Widget Demo
    p(class="banner-text") Kompatibel mit BundesIdent und echten Ausweisdokumenten.

block content
  div#useid-widget-container(data-tc-token-url=tcTokenUrl)
  script(async src=widgetSrc)