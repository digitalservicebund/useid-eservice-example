extends layout

block content
  h1 Identity process successfully completed
  h4 Received identity data:
  div
    for attribute in data
      li
        strong #{attribute.key}:&ensp;
        span= attribute.value
