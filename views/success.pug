extends layout

block content
  div(class="success-header")
    img(src="images/icon-success.svg", loading="lazy", class="success-icon", alt="Haken auf grünem Grund")
    h1(class="headline") Ausweis erfolgreich ausgelesen
  div(class="data")
    ul(class="data-attributes")
      for attribute in data
        li(class="data-attribute")
          span= attribute.value
